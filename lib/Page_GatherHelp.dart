import 'package:flutter/material.dart';
import 'Common_LogoAppBar.dart';

class GatherHelp extends StatelessWidget {
  const GatherHelp({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: LogoAppBar(),
    );
  }
}
