import 'package:flutter/material.dart';
import 'Common_LogoAppBar.dart';

class GatherMyPage extends StatelessWidget {
  const GatherMyPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: LogoAppBar(),
    );
  }
}
