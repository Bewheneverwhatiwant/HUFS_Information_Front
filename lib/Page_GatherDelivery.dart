import 'package:flutter/material.dart';
import 'Common_LogoAppBar.dart';

class GatherDelivery extends StatelessWidget {
  const GatherDelivery({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: LogoAppBar(),
    );
  }
}
