import 'package:flutter/material.dart';
import 'Common_LogoAppBar.dart';

class GatherTaxi extends StatelessWidget {
  const GatherTaxi({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: LogoAppBar(),
    );
  }
}
